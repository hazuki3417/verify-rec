import { IconAdCircle } from "./IconAdCircle";

/**
 * NOTE: 都度コンポーネントに結合しているIconを実装するのは手間なのでIconAdCircleを別名にしてそれをexportする
 *       名前だけ変えて使うイメージ。
 */
const IconX = IconAdCircle;
export { IconAdCircle, IconX };
